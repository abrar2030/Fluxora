apiVersion: v1
kind: Service
metadata:
  name: fluxora-frontend
  labels:
    app: fluxora-frontend
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: fluxora-frontend
