apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fluxora-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - hosts:
        - dev.fluxora.example.com
      secretName: fluxora-tls
  rules:
    - host: dev.fluxora.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: fluxora-backend
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fluxora-frontend
                port:
                  number: 80
