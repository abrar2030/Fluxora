apiVersion: v1
kind: Service
metadata:
  name: fluxora-database
  labels:
    app: fluxora-database
spec:
  type: ClusterIP
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
      name: mysql
  selector:
    app: fluxora-database
