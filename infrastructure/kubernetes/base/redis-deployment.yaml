apiVersion: apps/v1
kind: Deployment
metadata:
  name: fluxora-redis
  labels:
    app: fluxora-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fluxora-redis
  template:
    metadata:
      labels:
        app: fluxora-redis
    spec:
      containers:
        - name: fluxora-redis
          image: redis:6.2
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 6379
          resources:
            limits:
              cpu: 300m
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 256Mi
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 5
          volumeMounts:
            - name: fluxora-redis-data
              mountPath: /data
      volumes:
        - name: fluxora-redis-data
          persistentVolumeClaim:
            claimName: fluxora-redis-pvc
