#
# Fluxora Environment Variables
# Configuration for FastAPI application and MLOps tools (MLflow, Prefect, DVC)
#

# -----------------------------------------------------------------------------
# 1. APPLICATION SETTINGS
# -----------------------------------------------------------------------------
# Environment setting (e.g., development, staging, production)
FLUXORA_ENV=
# Port for the FastAPI application to run on
PORT=8000
# A secret key for signing tokens or other security-related needs
SECRET_KEY=

# -----------------------------------------------------------------------------
# 2. MLFLOW TRACKING
# -----------------------------------------------------------------------------
# URI for the MLflow Tracking Server.
# Example: http://localhost:5000 or postgresql://user:pass@host/db
MLFLOW_TRACKING_URI=
# Credentials for the MLflow Tracking Server (if required)
MLFLOW_TRACKING_USERNAME=
MLFLOW_TRACKING_PASSWORD=

# -----------------------------------------------------------------------------
# 3. PREFECT ORCHESTRATION
# -----------------------------------------------------------------------------
# URL for the Prefect API server
PREFECT_API_URL=
# API Key for authenticating with the Prefect server
PREFECT_API_KEY=

# -----------------------------------------------------------------------------
# 4. FEATURE STORE (Feast)
# -----------------------------------------------------------------------------
# URL for the Feast Core service (if remote)
FEAST_CORE_URL=
# Database connection string for the Feast Online Store (e.g., Redis, PostgreSQL)
# Example: redis://localhost:6379/0 or postgresql://user:pass@host/db
FEAST_ONLINE_STORE_URI=

# -----------------------------------------------------------------------------
# 5. DVC/CLOUD STORAGE (for model and data artifacts)
# -----------------------------------------------------------------------------
# AWS Credentials (if using S3 for DVC remote or Feast Offline Store)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
# Google Cloud Storage Credentials (if using GCS)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp/keyfile.json
# Azure Storage Credentials (if using Azure Blob Storage)
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
